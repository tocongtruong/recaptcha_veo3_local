services:
  captcha-solver-api:
    container_name: captcha-solver-api
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${API_PORT:-3000}:3000"
    environment:
      NODE_ENV: production
      API_PORT: ${API_PORT:-3000}
    restart: unless-stopped

  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: captcha-cloudflared-tunnel
    command: tunnel --no-autoupdate run --token ${TUNNEL_TOKEN}
    # Share network with API so Cloudflare origin http://localhost:3000 works reliably
    network_mode: "service:captcha-solver-api"
    restart: unless-stopped
    depends_on:
      - captcha-solver-api
